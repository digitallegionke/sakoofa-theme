{{ 'custom-styles.css' | asset_url | stylesheet_tag }}

<div class="announcement-bar">
  <div class="announcement-content">
    <p>Free shipping on all orders over €150</p>
  </div>
</div>

<header class="header">
  <nav class="nav-bar">
    <a href="/" class="logo">SAKOOFA</a>
    <div class="nav-menu">
      <a href="/collections/all" class="nav-link">SHOP</a>
      <a href="/collections/best-sellers" class="nav-link">BEST SELLERS</a>
      <a href="/blogs/journal" class="nav-link">JOURNAL</a>
      <a href="/pages/features" class="nav-link">FEATURES</a>
      <a href="/collections/lookbooks" class="nav-link">LOOKBOOKS</a>
      <a href="/search" class="nav-link">SEARCH</a>
      <a href="#" class="nav-link">UNITED STATES</a>
      <a href="/account/login" class="nav-link">LOGIN</a>
      <a href="/cart" class="nav-link cart-icon">
        <span class="cart-count">{{ cart.item_count }}</span>
      </a>
    </div>
  </nav>
</header>

<style>
.announcement-bar {
  background: #000;
  color: #fff;
  text-align: center;
  padding: 8px 0;
  font-size: 12px;
  font-family: 'DM Mono', monospace;
  white-space: nowrap;
  overflow: hidden;
}

.header {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  background: transparent;
}

.nav-bar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 20px 40px;
  background: rgba(255, 255, 255, 0.8);
  backdrop-filter: blur(10px);
}

.logo {
  color: #000;
  text-decoration: none;
  font-family: 'DM Mono', monospace;
  font-size: 24px;
  letter-spacing: -0.5px;
}

.nav-menu {
  display: flex;
  align-items: center;
  gap: 32px;
}

.nav-link {
  color: #000;
  text-decoration: none;
  font-family: 'DM Mono', monospace;
  font-size: 12px;
  letter-spacing: 0.5px;
  transition: opacity 0.2s ease;
}

.nav-link:hover {
  opacity: 0.7;
}

.cart-icon {
  position: relative;
  width: 20px;
  height: 20px;
}

.cart-count {
  position: absolute;
  top: -8px;
  right: -8px;
  background: #000;
  color: #fff;
  font-size: 10px;
  padding: 2px 6px;
  border-radius: 10px;
}

@media (max-width: 1024px) {
  .nav-menu {
    display: none;
  }
}
</style>

{% schema %}
{
  "name": "Header",
  "settings": [],
  "presets": [
    {
      "name": "Header",
      "category": "Header"
    }
  ]
}
{% endschema %}
